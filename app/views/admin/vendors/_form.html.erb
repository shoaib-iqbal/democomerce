<%= form_for(@admin_vendor) do |f| %>
  <% if @admin_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_vendor.errors.count, "error") %> prohibited this admin_vendor from being saved:</h2>

      <ul>
      <% @admin_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
