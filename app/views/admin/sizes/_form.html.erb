<%= form_for(@admin_size) do |f| %>
  <% if @admin_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_size.errors.count, "error") %> prohibited this admin_size from being saved:</h2>

      <ul>
      <% @admin_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>





